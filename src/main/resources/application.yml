server:
  port: 8080

spring:
  data:
    redis:
      host: 127.0.0.1
      port: 6379
management:
  endpoints:
    web:
      exposure:
        include: "*"
  metrics:
    enable:
      all: true
  endpoint:
    prometheus:
      enabled: true
